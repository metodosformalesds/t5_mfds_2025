version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Frontend_WS
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: Frontend_WS/build
    files:
      - '**/*'
  cache:
    paths:
      - Frontend_WS/node_modules/**/*